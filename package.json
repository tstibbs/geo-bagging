{
	"name": "@tstibbs/geo-bagging-root",
	"private": true,
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": "22",
			"onFail": "error"
		},
		"packageManager": {
			"name": "pnpm",
			"version": ">=11.7",
			"onFail": "error"
		}
	},
	"devDependencies": {
		"lint-staged": "^15.2.2",
		"prettier": "^3.2.5"
	},
	"scripts": {
		"prepare": "git config core.hooksPath .project/hooks"
	}
}
